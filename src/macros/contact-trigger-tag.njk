{% macro contactTriggerTag(text, isButton=false, subject='Work with Alaris Prime', message, additionalClasses) %}{% set classNames = 'link link--minor' %}{% if isButton %}{% set classNames = 'button button--primary' %}{% endif %}<a class="{{ classNames }} {{ additionalClasses }} js-overlay-open" href="javascript:;" data-subject="{{ subject }}"{% if message %} data-message="{{message}}"{% endif %}>{{ text}}</a>{% endmacro %}
